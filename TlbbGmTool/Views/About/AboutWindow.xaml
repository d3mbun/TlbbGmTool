<Window x:Class="liuguang.TlbbGmTool.Views.About.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:liuguang.TlbbGmTool.Views.About"
        xmlns:vm="clr-namespace:liuguang.TlbbGmTool.ViewModels"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="CanMinimize"
        Title="Thông tin chương trình" Height="430" Width="640">
    <Window.DataContext>
        <vm:AboutWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type TextBlock}" x:Key="BoldBlock">
                <Setter Property="HorizontalAlignment" Value="Center" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="FontWeight" Value="Bold" />
            </Style>
            <Style TargetType="{x:Type TextBlock}" x:Key="CommonBlock">
                <Setter Property="FontSize" Value="12" />
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="60" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Style="{StaticResource BoldBlock}">
            <Run Text="Công cụ này được phát triển bởi Lưu Quang, chỉ dùng cho nghiên cứu kỹ thuật."/>
            <LineBreak/>
            <Run Text="Mọi hậu quả khi sử dụng công cụ này tác giả không chịu trách nhiệm."/>
        </TextBlock>
        <TextBlock Grid.Row="1" Style="{StaticResource BoldBlock}">
            <Run>Phiên bản hiện tại:</Run>
            <Run Foreground="#FF51ED1A">[</Run>
            <Run Foreground="#FF3198DA" Text="{Binding Version}"/>
            <Run Foreground="#FF51ED1A">] </Run>
            <Run> Runtime:</Run>
            <Run Foreground="#FF51ED1A">[</Run>
            <Run Foreground="#FF3198DA" Text="{Binding AppRuntime}"/>
            <Run Foreground="#FF51ED1A">]</Run>
        </TextBlock>
        <TextBlock Grid.Row="2" Style="{StaticResource BoldBlock}">
            <Run Text="Trang chủ dự án："/><Hyperlink NavigateUri="{Binding RepositoryUrl}" RequestNavigate="Hyperlink_RequestNavigate" ToolTip="Truy cập trang chủ dự án"><Run Text="{Binding RepositoryUrl}"/></Hyperlink>
        </TextBlock>
        <GroupBox Grid.Row="3" Margin="20,0,20,20" Padding="15">
            <GroupBox.Header>
                <Label Content="Hướng dẫn sử dụng"/>
            </GroupBox.Header>
            <TextBlock TextWrapping="Wrap">
                <Run Text="1. Click Menu - Quản lý Server, thêm thông tin cấu hình cơ sở dữ liệu của máy chủ."/><LineBreak/>
                <Run Text="2. Chức năng Quản lý Nhân vật cần đọc dữ liệu máy khách, sau khi click Kết nối, vui lòng đợi một lát để dữ liệu tải thành công mới có thể sử dụng tìm kiếm."/><LineBreak/>
                <Run>3. Không phải trang bị nào cũng có thuộc tính, tùy thuộc vào file EquipBase.txt và ItemSegValue.txt. Tối đa 16 loại thuộc tính.</Run><LineBreak/>
                <Run Text="4. Chức năng sửa đổi của công cụ này không ảnh hưởng toàn cục, chỉ sửa đổi bản ghi dữ liệu cụ thể."/><LineBreak/>
                <Run Text="5. Nếu phân tích file txt thất bại, bạn có thể tạo thư mục Config trong thư mục chứa Config.axp và đặt file txt vào đó."/><LineBreak/>
                <LineBreak/>
                <Run Foreground="Red" Text="Lưu ý: Khi sửa đổi dữ liệu, cần thoát game trước, nếu không sửa đổi có thể không có hiệu lực."/>
            </TextBlock>
        </GroupBox>
    </Grid>
</Window>